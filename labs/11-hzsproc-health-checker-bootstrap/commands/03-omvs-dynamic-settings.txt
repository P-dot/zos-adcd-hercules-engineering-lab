# SDSF LOG - con barra

/SETOMVS MAXFILEPROC=64000
/D OMVS,O

# Miembro USER.PARMLIB(BPXPRMT9)
NETWORK DOMAINNAME(AF_INET)
        DOMAINNUMBER(2)
        MAXSOCKETS(64000)
        TYPE(INET)

# Activacion dinamica
/SETOMVS RESET=(T9)
/D OMVS,O

# Reejecucion del check
/F HZSPROC,RUN,CHECK=(IBMUSS,USS_MAXSOCKETS_MAXFILEPROC)
