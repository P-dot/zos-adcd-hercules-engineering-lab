# Fuente de evidencia

Las capturas de este lab proceden del documento de evidencia `JESQ.docx` aportado durante la sesión.

El documento muestra:

- `$HASP050 JES2 RESOURCE SHORTAGE OF JQES - 81% UTILIZATION REACHED`.
- `D A,L` con JES2 y started tasks principales activos.
- `$D JOBDEF` antes/después.
- `$D SPOOL` con `SBSYS1` y `SBSYS9` activos.
- `$D JOBQ` mostrando salidas de started tasks en `AWAITING HARDCOPY`.
- `$T JOBDEF,JNUMWARN=90` aplicado.
