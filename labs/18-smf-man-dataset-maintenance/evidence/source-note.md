# Source Note

Evidence comes from the user-provided `SMF(3).docx` screenshots captured during the live z/OS ADCD/Hercules lab.

The final evidence shows `D SMF` with MAN datasets cleared and `D R,L` reporting no messages outstanding.
